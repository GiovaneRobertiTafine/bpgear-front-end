import { TransformValuePipe } from './transform-value.pipe';

describe('TransformValuePipe', () => {
  it('create an instance', () => {
    const pipe = new TransformValuePipe();
    expect(pipe).toBeTruthy();
  });
});
